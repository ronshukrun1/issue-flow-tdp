NODE_ENV=development

# PostgreSQL connection (matches compose.yml)
DB_HOST=localhost
DB_PORT=5432
DB_USERNAME=issueflow
DB_PASSWORD=issueflow
DB_NAME=issueflow
DB_SYNCHRONIZE=true

# JWT
JWT_SECRET=change-me-to-a-strong-random-secret
JWT_EXPIRATION=3600
